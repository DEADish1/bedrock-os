import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Bedrock Server OS — The foundation for everything you run',
  description: 'Store files, run virtual machines, and manage your server from one clear interface.',
  icons: { icon: '/brand/favicon.svg' },
  openGraph: { title: 'Bedrock Server OS', description: 'The foundation for everything you run.', images: ['/brand/social-card-1200.png'] },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
